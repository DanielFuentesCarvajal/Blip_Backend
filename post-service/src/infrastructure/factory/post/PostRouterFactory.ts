import PostController from "../../../api/controller/PostController";
import PostRouterInterface from "../../../api/interface/PostRouterInterface";
import PostRouter from "../../../api/routes/PostRouter";

export default class PostRouterFactory {
   /* public static create(): PostRouterInterface{

        // se instancian los servicios
        

        // Crear los casos de uso inyectando el servicio de citas
        


        ////inyecta las dependencias en el controlador
        const postController = new PostController()


        return new PostRouter(articleController)
    }*/
}