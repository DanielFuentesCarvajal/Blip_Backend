import { Mock, MockConfig } from './types';
export declare function mock<T>(clazz?: {
    new (...args: any[]): T;
}, mockConfig?: MockConfig): Mock<T>;
export declare function mock<T>(mockConfig?: MockConfig): Mock<T>;
export { Mock, MockConfig, TsJestMocker } from './types';
